import { ComponentFixture, TestBed } from "@angular/core/testing";

import { DevConfigComponent } from "./dev-config.component";

/*
describe('DevConfigComponent', () => {
  let component: DevConfigComponent;
  let fixture: ComponentFixture<DevConfigComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ DevConfigComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(DevConfigComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
