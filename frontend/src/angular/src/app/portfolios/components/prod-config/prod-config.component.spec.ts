import { ComponentFixture, TestBed } from "@angular/core/testing";

import { ProdConfigComponent } from "./prod-config.component";
/*
describe('ProdConfigComponent', () => {
  let component: ProdConfigComponent;
  let fixture: ComponentFixture<ProdConfigComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ProdConfigComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ProdConfigComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
