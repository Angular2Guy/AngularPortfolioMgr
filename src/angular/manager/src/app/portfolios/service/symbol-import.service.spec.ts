import { TestBed } from '@angular/core/testing';

import { SymbolImportService } from './symbol-import.service';

/*describe('SymbolImportService', () => {
  let service: SymbolImportService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(SymbolImportService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});*/
